[Unit]
Description=Instagram Webhook Bridge for OpenClaw
After=network.target

[Service]
Type=simple
WorkingDirectory=/path/to/instagram-webhook
EnvironmentFile=/path/to/instagram-webhook/.env
ExecStart=/usr/bin/node index.js
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
